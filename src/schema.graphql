# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

type AuthPayload {
  name: String!
  token: String!
}

type Comment {
  createdAt: DateTimeISO!
  id: Int!
  message: String!
  postId: Int!
  updatedAt: DateTimeISO!
  userId: Int!
}

input CreateCommentInput {
  message: String!
  postId: Int!
}

input CreatePostInput {
  content: String!
  title: String!
}

input CreateTagInput {
  name: String!
}

"""
A date-time string at UTC, such as 2007-12-03T10:15:30Z, compliant with the `date-time` format outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for representation of dates and times using the Gregorian calendar.This scalar is serialized to a string in ISO 8601 format and parsed from a string in ISO 8601 format.
"""
scalar DateTimeISO

type Follow {
  createdAt: DateTimeISO!
  followerId: Int!
  followingId: Int!
}

type HashTag {
  createdAt: DateTimeISO!
  id: Int!
  name: String!
}

input InputId {
  id: Int!
}

type LikePost {
  createdAt: DateTimeISO!
  postId: Int!
  userId: Int!
}

input LoginInput {
  email: String!
  password: String!
}

type Mutation {
  createComment(input: CreateCommentInput!): Comment!
  createHashTag(input: CreateTagInput!): HashTag!
  createPost(input: CreatePostInput!): Post!
  deleteComment(input: InputId!): Comment!
  deletePost(input: InputId!): Post
  follow(input: InputId!): Follow!
  likePost(input: InputId!): LikePost!
  login(input: LoginInput!): AuthPayload
  signup(input: SignupInput!): AuthPayload
  unfollow(input: InputId!): Follow!
  updateComment(input: UpdateCommentInput!): Comment!
  updatePost(input: UpdatePostInput!): Post
}

type Post {
  authorId: Int!
  content: String!
  createdAt: DateTimeISO!
  id: Int!
  title: String!
}

type Query {
  getComments: [Comment!]!
  getFollowings(input: InputId!): [Follow!]!
  getHello: String!
  getPost(input: InputId!): Post
}

input SignupInput {
  email: String!
  name: String!
  password: String!
}

input UpdateCommentInput {
  commentId: Int!
  message: String!
}

input UpdatePostInput {
  content: String
  id: Int!
  title: String
}